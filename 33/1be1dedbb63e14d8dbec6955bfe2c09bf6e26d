Completes the Linux maintenance boundary exposed by CI: the shared voice module explicitly allows dead code and unused imports only on Linux because the feature is intentionally unavailable there, while macOS and Windows remain warning-strict. Removes the Linux-only needless return. Verified with cargo fmt --check, cargo check, cargo clippy --all-targets -- -D warnings, git diff --check, and graphify update .; Linux CI is restarted for proof.
