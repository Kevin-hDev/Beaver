Windows CEF diagnosis checkpoint. The latest packaged journey proves the official extension Host reaches running; the first failure is set_extension_enabled for the first local fixture. This commit extends the short Windows job to test the missing native boundaries using Beaver's prepared bundled Node and OwnedProcess path: confirmed stop followed by restart, and two simultaneously alive isolated Hosts. The workflow deliberately runs these before the hour-long CEF build so the next result distinguishes Windows Job Object/process lifecycle failure from the higher-level runtime sync/load path. Local verification: 3 filtered Rust tests passed with windows-tests; scripts/e2e/e2e-process.test.mjs 27 passed; cargo fmt --check and git diff --check passed. No production behavior changed.
