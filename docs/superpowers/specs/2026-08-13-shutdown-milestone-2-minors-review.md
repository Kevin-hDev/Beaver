Exit code: 0
Wall time: 0.2 seconds
Output:
# Review — lot des corrections mineures du jalon 2

Date : 2026-08-13
Branche : `codex/shutdown-milestone-2-minors`, plage `4f5702a..5915271` (22 commits, 111 fichiers, ~3 500 insertions), 22 git notes lues.
CI verte sur `5915271` (CI 3 OS, CodeQL, audit des dépendances). Design du lot : `2026-08-12-shutdown-milestone-2-minors-design.md` (35 obligations).
Méthode : review du cœur en lecture directe, quatre reviews par domaine (gateway, scheduler+i18n, OAuth+MCP, sidecars+GPU+SearXNG), chaque constat majeur re-vérifié dans le code avant consignation.

## Verdict

**27 obligations sur 35 corrigées et vérifiées, souvent proprement. Mais six défauts nouveaux ou amplifiés par ces corrections sont à corriger avant fusion, plus une obligation non corrigée dont l'audit affirme le contraire.** Deux des six touchent une fonctionnalité visible (historique des réveils faux, canal Discord tuable définitivement).

## À corriger avant fusion

1. **Un réveil réussi peut être journalisé « raté », et son vrai résultat est perdu** — `scheduler/runtime.rs:153` lance le réveil en arrière-plan sans écrire de décision « en cours » ; le curseur reste volontairement 5 min en arrière (`checkpoint(cutoff)`, `runtime.rs:124`) ; toute réconciliation pendant une exécution longue (rechargement de config, plafond de sommeil 60 min, autre réveil) re-propose l'occurrence, ne trouve aucune entrée au journal, et écrit `missed`. À la fin du run, `log_ok` est avalé par la déduplication (`log_store.rs:96-98` rend `Ok(())` sans écrire) : l'historique affiche « raté » pour un run réussi, session et tokens jamais journalisés. Introduit par `1176a5d` (avant, `checkpoint(now)` passait l'occurrence derrière le curseur immédiatement). Correction : donner une décision aux occurrences en vol (journaliser au lancement, ou consulter le registre de travail dans la réconciliation).
2. **Le journal des réveils est relu et réécrit en entier à chaque décision en régime permanent** — le journal est glissant à 500 lignes : c'est l'état normal d'une installation existante. `rotated_content` garde 499 + 1 = 500 lignes (`log_store.rs:168`), donc `line_count` retombe exactement au seuil et `needs_rotation` (`log_metadata.rs:55`) est vrai au prochain append : relecture jusqu'à 1 Mo + reparse de 500 JSON + réécriture complète, à chaque décision. L'obligation 20 n'est tenue que pour un journal < 500 lignes. Même famille que le trim de l'audit gateway corrigé en re-review du jalon (garder une marge après rotation, p. ex. 250 lignes). Le test `log_tests.rs:124-146` s'arrête au 501ᵉ append — le 502ᵉ aurait montré le cycle.
3. **Un échec transitoire d'IDENTIFY Discord consomme le budget définitif de redémarrage** — `discord_events.rs:78` fait porter trois significations au même booléen : consommateur fermé, échec d'envoi de l'IDENTIFY, échec de zéroïsation. Les trois produisent `break 'gateway` (`discord.rs:141`) → fin de la tâche → un des 10 redémarrages du superviseur (GiveUp au 11ᵉ en 300 s → canal `Error` définitif). Avant : `break` interne → reconnexion locale, sans budget. Une indisponibilité Discord de quelques minutes peut tuer le canal jusqu'au redémarrage du gateway ; un échec de zéroïsation détruit une connexion saine. Correction : séparer les issues (`Closed` → `break 'gateway` ; échec d'envoi → boucle de reconnexion ; échec de zéroïsation → trace).
4. **La remise à zéro du backoff de reconnexion est inatteignable si des trames non-Text arrivent avant 60 s** — `slack.rs:122` et `discord.rs:123` cassent la boucle sur toute trame `Ping`/`Pong`/`Binary`/`Close` (préexistant), et la politique ne se réinitialise qu'après 60 s de connexion (`reconnect_policy.rs:25-29`). Si Slack émet ses pings WebSocket sous 60 s, chaque cycle enregistre une connexion courte, le délai plafonne à 30 s en permanence, et le canal est indisponible la majorité du temps (avant : reconnexion fixe 5 s — le lot amplifie un défaut préexistant). Réserve : fréquence réelle des pings Slack non observée ; le mécanisme, lui, est certain en lecture. Correction : ignorer `Ping`/`Pong` (`continue`) et ne sortir que sur `Close`/erreur.
5. **Les refus d'admission du consommateur gateway ne sont pas comptés** — `service_consumer.rs:61` appelle `try_record` (écrit seulement) au lieu de `record_refusal` (compte + écrit) : `refused_messages` manque les pertes par saturation de traitement, et audit désactivé, cette perte redevient invisible — le trou exact que l'obligation 16 devait fermer. Une ligne.
6. **Le serveur de callback OAuth MCP se fige sur une connexion muette** — `mcp_oauth/callback_server.rs:67-76` : accept séquentiel, `read` sans délai par connexion. Une socket locale ouverte silencieuse bloque la boucle ; le vrai callback du navigateur n'est jamais lu ; échec à l'échéance de 300 s — le symptôme que l'obligation 22 devait éliminer, par un autre chemin. Le serveur jumeau Codex règle exactement ce cas (handlers concurrents en `JoinSet` + délai 5 s par connexion) : aligner.
7. **Obligation 28 non corrigée, audit inexact** — la couture d'injection d'échec `UninstallBoundary`/`fail_if_requested` (`forecast/model_manager/uninstall.rs:6,28,57`) reste compilée dans le binaire livré ; aucun commit du lot ne touche ce fichier. La note de `844e171` le dit honnêtement (« reste pour son lot dédié ») mais l'audit final de `bb2ec62` affirme « 27-28 corrigé par 844e171 » et « aucune obligation sans correctif ». Corriger la couture (quelques `#[cfg(test)]`) ou corriger l'audit — pas laisser les deux affirmations contradictoires.

## Obligations partiellement tenues

- **15** — le refus non journalisé ne perd plus l'occurrence sur le chemin nominal (rattrapée en `missed`). Mais la branche pause globale (`runtime.rs:44`) checkpointe à `now` sans condition : une occurrence dont l'écriture du refus vient d'échouer est perdue définitivement si l'utilisateur active la pause. Et un échec de `claim_once` (écriture config) bloque le checkpoint avec le message trompeur « journal indisponible » (`runtime_decisions.rs`), curseur figé tant que la config est illisible.
- **16** — compteur saturant présent, incrémenté avant l'écriture optionnelle (juste) ; mais le consommateur n'y passe pas (défaut 5) et aucun composant n'affiche `refused_messages` (le champ traverse l'IPC et `use-channels.ts` puis meurt) : pour l'utilisateur, la perte reste invisible.
- **18** — cinq catégories fermées présentes, mais `service_audit.rs:73` jette le contenu de l'erreur (`error.map(|_| …)`) et dérive la catégorie de deux champs déjà sérialisés dans la même ligne : information nette nulle. Et une seconde table de catégorisation (`sanitize_error`/`from_stable_reason`) vit sur le chemin `agent_bridge_support::audit_msg` — deux autorités qui divergeront au premier ajout.
- **19** — politique 1/2/4/8/16/30 s correcte et annulable ; mais la remise à zéro est inatteignable (défaut 4) et la première tentative Slack passe de 10 s à 1 s sur un endpoint limité en débit.
- **20** — voir défaut 2 : tenue seulement sous 500 lignes.
- **21** — le statut est traduit et le texte français brut n'est plus exposé ; mais le repli texte du frontend est mort (le backend `skip_serializing` le champ `error` : `wakeup-run-error.ts:34` inatteignable, le type TS `error?: string` déclare un champ jamais envoyé) et la documentation de compatibilité demandée n'existe nulle part.
- **31** — le désalignement 3 s / 4 s est corrigé via `HEALTH_PROBE_BUDGET` ; mais le second consommateur (`sidecar_spawn.rs:105-118`, 60 appels `health_info` synchrones sur worker Tokio) ignore le budget (préexistant), et l'écriture n'a pas de timeout — le budget n'est pas strictement la somme des phases.

## Vérifié conforme (résumé)

Obligations 1-9, 11-14, 17, 22-27, 29, 30, 32-35 : corrigées et vérifiées en lecture directe. À souligner : `verified_orphan_pid` prend un `FnOnce` — la double sonde est impossible à la compilation ; le `Drop` d'`OAuthCompletionOwner` répare au passage un blocage préexistant (login abandonné laissait `ACTIVE_LOGIN` bloqué) ; `PendingSidecar` ferme la fenêtre spawn→publication y compris sur panique ; le test MCP d'arrêt vérifie la disparition d'un PID réel ; la couture sous-agents ne remplace plus la règle (la limite réelle de 8 est exercée), seul le périmètre de comptage change en test, avec le commentaire de contrainte.

## Nouveaux mineurs (à reprendre après la fusion)

- Gateway : `RefusalAuditOutcome` sans `#[must_use]` et compteur `dropped` invisible en production (`let _ = record_refusal`, `backpressure.rs:23,27`) ; `refused_messages` recréé à zéro à chaque `start()` sans indication ; test `disabled_persistent_audit_still_counts_refused_messages` qui ne désactive jamais l'audit ; boucle de production `refusal_audit::run` couverte par aucun test (le test passe par `run_with_writer_for_test`).
- Scheduler : validation du cache par taille seule (`log_store.rs:82`) — un remplacement externe de taille identique fait rendre `Ok(())` pour une décision jamais écrite ; fichier > 1 Mo ou UTF-8 invalide → cache reconstruit à chaque append en silence ; éviction FIFO (non LRU) du cache 16 chemins, bénin aujourd'hui ; un `once` désactivé entre la lecture de config et la revendication reçoit une entrée `missed` (perte du cas silencieux, compromis non documenté) ; `claim_once` fait des E/S bloquantes sous verrou synchrone sur la tâche async (préexistant).
- OAuth/MCP : `verify_state_constant_time` maison alors que Codex utilise `subtle::ct_eq` — deux autorités de comparaison, une seule conforme à la règle projet ; 50 connexions vides suffisent à tuer un flux (`MAX_ATTEMPTS` compte les rejets) ; état validé deux fois (`wait` + `flow.rs:100`) ; `stdio_session.rs:48` sort par `?` avant `abort_all` (invariant tenu par coïncidence) ; `JoinSet`+oneshot superflu vs `select!`, avec `tokio::spawn` nu contre la convention du projet ; `BEAVER_MCP_INIT_DELAY_MS` transite par `env_tokens`, canal réservé aux secrets ; créneau OAuth jamais vidé après complétion + course étroite cancel/login concurrents ; tests d'`OAuthCompletion` n'exerçant que le `Drop` ; en-têtes `Cache-Control: no-store` absents côté MCP (Codex les pose).
- Sidecars/GPU/SearXNG : `ensure_running` SearXNG rend `Ok(url)` et journalise « démarré » pour un processus que la fermeture vient de tuer (`start_lifecycle.rs:51-60`) ; `PendingSidecar::drop` tue le PID seul, pas le groupe, et `release` avant moisson ; `save_pid` Forecast désormais après publication (état durable après l'action — inversé) et échec de persistance SearXNG avalé (`let _ =`, `start_lifecycle.rs:98`) ; `SystemRoot` validé faiblement (env var contrôlable par l'utilisateur même ; l'API sûre serait `GetSystemDirectoryW`) et erreurs de résolution jamais tracées — la sonde d'identité rend alors le diagnostic faux « n'est plus le sidecar » et l'orphelin devient introuvable ; `terminate_tokio` non borné sur la branche annulation de la sonde GPU ; port Forecast dans un `AtomicU16` global, seconde autorité tenue par la seule discipline `lock_prediction`.
- Cœur : mutation de session (`prepare_send`, persistance du mode) et remplacement de flux exécutés avant le contrôle d'admission — fenêtre pendant la fermeture, contenue par la barrière globale (l'ancien commentaire d'ordre a été supprimé sans remplacement de la propriété) ; tests par `include_str!` comptant les occurrences de texte source (mascotte, runtime_startup) — fragiles au moindre commentaire ; comptage par thread du registre sous-agents faussé si un test passe un jour en runtime multi-thread ; `installer_tests.rs` ne couvre que la garde, pas le chemin complet annulé.

## Exactitude des notes (2 écarts sur 22)

- `e6547c0` affirme l'effacement de toutes les copies du jeton Discord : les copies applicatives le sont, mais tungstenite garde une copie interne du cadre masqué (`out_buffer`) non effaçable sans patcher la bibliothèque — à borner dans la formulation, pas corrigeable ici.
- `4a1377f` annonce « fichiers de production maximum 192 lignes » ; `start_lifecycle.rs` (ajouté par `b36b940`, qui le dit correctement) fait 217 lignes, dont ~99 de coutures `#[cfg(test)]`.
- Et l'audit final de `bb2ec62` sur l'obligation 28 (défaut 7).

## Critères de fusion du lot

1. Les défauts 1 à 6 corrigés (le 5 est une ligne ; le 3, le 4 et le 7 quelques lignes ; le 6 un alignement sur le serveur Codex existant ; le 1 et le 2 demandent une petite conception).
2. La contradiction de l'obligation 28 tranchée (correctif ou audit corrigé).
3. CI verte sur les trois OS après le lot de corrections.
4. Les nouveaux mineurs ci-dessus rejoignent la liste à reprendre après la fusion.
