Review remediation for REVUE_IMPLEMENTATION_EXTENSIONS_2026-09-02.md.

Blockers closed: event broadcast excludes revoked/stopping hosts and invalidation defers to an active stop owner; Plan mode now admits only read-only extension effects through the real guard; the Host panel now distinguishes initial loading, refresh error, and validated content without inventing a stopped status.

Important findings closed: canonical host-unavailable error; restart budget charged only on real automatic spawn; failed pre-bind processes are killed or retained with their channel directory; concurrent stop completion recognizes removal/replacement; recovery existence uses the full registry, retry attempts survive neighboring loads, oversized markers require explicit discard; Rust-to-frontend host status is strictly parsed against generated states, stages, codes, identifiers, bounds, and collection limits.

Minor hardening: contract codes and user identifiers have separate generated limits; load-stage method and values are generated; operation reasons are sanitized before every log sink; dead lifecycle state removed; Windows extension hosts are created suspended, admitted to a dedicated Job, then resumed; extension grants require an explicit cache flag; MCP uses the generated outer deadline only; corrupt bounded journals reset with a generic diagnostic; repeated sensitive-access writes short-circuit; v1 registry reads tolerate unknown envelope fields; migration validates the projected v1 before exact backup; fingerprint revocation reasons survive disable; the reserved @manifest entrypoint is rejected; real diagnostic deserialization is tested; diagnostic identifiers are never cut mid-value; extension dates are localized.

A2-07 intentionally unchanged: installer_record::for_update makes the replacement disabled, untrusted, and fingerprintless before registry::replace_user publishes it. Tool routing indexes only enabled and trusted records, so publication closes new dispatch before revoke; the old process is retained only until confirmed stop and cannot receive calls under the new record.

Self-review additions: retained pre-bind hosts are now reaped when their reader reports requested exit; the remaining frontend recovery parser literal 96 was replaced by the generated contract limit; runtime lifecycle supervision was split so every changed production/test-independent source stays at or below 230 lines. No new user-visible string was introduced, so the seven locale catalogs were not changed.

Validation: npm run lint PASS; npx tsc --noEmit PASS; npm test PASS (2867 Vitest, 56 build-script, 16 CI-script, 45 CEF plus 1 Windows-only skip, 70 E2E-runner, 57 extension-host, 10 reasoning fixture tests); cargo fmt --check PASS; cargo check PASS; cargo clippy --all-targets -- -D warnings PASS; cargo test PASS (4726 library tests, 10 ignored, plus 1 integration test); cargo check --lib --target x86_64-pc-windows-gnu --features windows-tests PASS. graphify update . completed. The macOS test linker emitted its pre-existing __eh_frame size warning; the Windows cross-library build emitted pre-existing CEF cfg/dead-code warnings. Native visual UI validation was not performed in this correction lot.
