Context: CI run 33960750267 failed only in the isolated Windows private-storage crate after private_store.rs began consuming the shared windows_fs_retry module. Root cause: the miniature crate mirrored crate::services::paths but not crate::services::windows_fs_retry, so the production module could not compile in that harness. Correction: include the real production retry module once at the crate root and re-export it through the same crate::services path used by Beaver; no runtime behavior is duplicated or changed. Validation: cargo check --manifest-path src-tauri/tests/windows-private-store/Cargo.toml --target x86_64-pc-windows-msvc; cargo fmt --check for the isolated manifest; git diff --check; graphify incremental code-only update completed. The cross-target check reports only pre-existing unused production re-export warnings in the intentionally small harness.
