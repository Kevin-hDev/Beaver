// One domain budget covers discovery, installation and final loading so every
// accepted skill manifest remains loadable throughout its lifecycle.
pub const MAX_SKILL_MANIFEST_BYTES: usize = 256 * 1024;
