{
  "version": 1,
  "skill": "ticket-info",
  "cases": [
    { "id": "lookup-by-id", "should_trigger": true, "messages": [{ "role": "user", "content": "Show me ticket BEA-142." }], "expect": { "contains": ["BEA-142", "status", "URL"], "writes_files": false }, "judge": "Every displayed value must come from the configured connector record for exactly BEA-142 and the lookup must remain read-only.", "must_not": ["modify the ticket"] },
    { "id": "lookup-by-url", "should_trigger": true, "messages": [{ "role": "user", "content": "Read the details from this configured tracker ticket URL." }], "expect": { "contains": ["title", "description", "status"], "writes_files": false }, "must_not": ["follow an unconfigured host"] },
    { "id": "infer-from-branch", "should_trigger": true, "messages": [{ "role": "user", "content": "Show the ticket for my current branch." }], "expect": { "contains": ["branch", "identifier", "ticket"], "writes_files": false }, "must_not": ["guess from multiple identifiers"] },
    { "id": "include-comments", "should_trigger": true, "messages": [{ "role": "user", "content": "Show BEA-142 and all 230 comments in its discussion using bounded continuation pages." }], "expect": { "contains": ["Page 1", "Page 2", "Page 3", "230", "continuation"], "writes_files": false }, "judge": "The response must process all 230 requested comments in pages of at most 100, preserve stable ordering, and account for continuation without accumulating or silently truncating the result.", "must_not": ["fetch more than 100 comments in one page", "post a comment"] },
    { "id": "include-relationships-in-pages", "should_trigger": true, "messages": [{ "role": "user", "content": "Show BEA-142 and all 45 linked relationships using bounded pages." }], "expect": { "contains": ["Page 1", "Page 2", "Page 3", "45"], "writes_files": false }, "judge": "All 45 requested relationships must be covered in pages of at most 20 with no duplicate and no mutation.", "must_not": ["fetch more than 20 relationships in one page"] },
    { "id": "ambiguous-branch", "should_trigger": true, "messages": [{ "role": "user", "content": "Show the ticket for this branch, whose name contains both BEA-12 and OPS-8." }], "expect": { "contains": ["ambiguous"], "waits_for_user": true, "writes_files": false }, "must_not": ["query both silently"] },
    { "id": "missing-tracker", "should_trigger": true, "messages": [{ "role": "user", "content": "Look up ticket 142, but this project has no configured tracker." }], "expect": { "contains": ["tracker unavailable", "blocked"], "writes_files": false }, "must_not": ["search credentials", "guess a provider"] },
    { "id": "create-ticket", "should_trigger": false, "messages": [{ "role": "user", "content": "Create a new bug ticket for this crash." }], "expect": { "writes_files": false }, "judge": "A creation request must not trigger a read-only ticket lookup.", "must_not": ["use a read-only lookup"] },
    { "id": "comment-ticket", "should_trigger": false, "messages": [{ "role": "user", "content": "Post this investigation result as a comment on BEA-142." }], "expect": { "writes_files": false }, "must_not": ["post the comment"] },
    { "id": "implement-ticket", "should_trigger": false, "messages": [{ "role": "user", "content": "Implement ticket BEA-142 and run its tests." }], "expect": { "writes_files": false }, "must_not": ["stop after displaying fields"] },
    { "id": "general-status", "should_trigger": false, "messages": [{ "role": "user", "content": "What does backlog status usually mean in agile teams?" }], "expect": { "writes_files": false }, "must_not": ["query a tracker"] }
  ]
}
