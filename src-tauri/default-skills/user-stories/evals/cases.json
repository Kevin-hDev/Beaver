{
  "version": 1,
  "skill": "user-stories",
  "cases": [
    { "id": "split-epic", "should_trigger": true, "messages": [{ "role": "user", "content": "Split this offline export epic into prioritized INVEST user stories." }], "expect": { "contains": ["user outcome", "acceptance criteria", "priority"], "writes_files": false }, "judge": "Every story must be a vertical user-visible outcome, satisfy INVEST or be explicitly not ready, and carry observable acceptance and functional completion conditions.", "must_not": ["slice by frontend and backend"] },
    { "id": "single-story", "should_trigger": true, "messages": [{ "role": "user", "content": "Write one story for renaming a saved note." }], "expect": { "contains": ["As a", "acceptance criteria", "functional completion"], "writes_files": false }, "must_not": ["invent an epic"] },
    { "id": "estimate-without-policy", "should_trigger": true, "messages": [{ "role": "user", "content": "Estimate and prioritize these stories; the team has no point system." }], "expect": { "contains": ["relative effort", "confidence"], "writes_files": false }, "must_not": ["invent person-days"] },
    { "id": "tracker-explicit-awaits-approval", "should_trigger": true, "messages": [{ "role": "user", "content": "Create these five ready stories in the configured tracker." }], "expect": { "contains": ["complete ready backlog"], "waits_for_user": true, "writes_files": false }, "judge": "The skill must show the complete ready backlog and wait for explicit approval immediately before any tracker write.", "must_not": ["create blocked stories", "write before approval"] },
    { "id": "missing-user-outcome", "should_trigger": true, "messages": [{ "role": "user", "content": "Create stories for the new database layer." }], "expect": { "contains": ["user outcome"], "waits_for_user": true, "writes_files": false }, "must_not": ["make database tasks into user stories"] },
    { "id": "scope-assumption-not-ready", "should_trigger": true, "messages": [{ "role": "user", "content": "Draft export stories, but we have not decided whether users export all notes or a selection." }], "expect": { "contains": ["not ready", "scope assumption"], "writes_files": false }, "must_not": ["declare the backlog ready under an assumption"] },
    { "id": "oversized-backlog", "should_trigger": true, "messages": [{ "role": "user", "content": "Create all 80 accepted user stories for this program in bounded continuation batches." }], "expect": { "contains": ["Batch 1", "Batch 2", "Batch 3", "Batch 4", "80"], "writes_files": false }, "judge": "The union of four or more batches must cover all 80 accepted outcomes exactly once while preserving global dependencies and final ranking; the skill must not stop after 20.", "must_not": ["drop outcomes after the first batch", "rank each batch independently without reconciliation"] },
    { "id": "tracker-multiple-batches", "should_trigger": true, "messages": [{ "role": "user", "content": "Create these 45 ready stories in the configured tracker in safe batches." }, { "role": "assistant", "content": "Here is the complete ready ranked backlog of 45 stories. Approve external creation?" }, { "role": "user", "content": "Approved. Create all 45 now." }], "expect": { "contains": ["Batch 1", "Batch 2", "Batch 3", "identifiers", "URLs"], "writes_files": false }, "judge": "After explicit approval, the skill must create at most 20 stories per external batch, verify every identifier and URL, search duplicates, and account for all 45 unless a reported failure stops continuation.", "must_not": ["create more than 20 in one batch", "retry an ambiguous create blindly"] },
    { "id": "write-prd", "should_trigger": false, "messages": [{ "role": "user", "content": "Write the product requirements document for this idea." }], "expect": { "writes_files": false }, "judge": "A PRD authoring request must not trigger story backlog generation.", "must_not": ["skip product definition"] },
    { "id": "technical-plan", "should_trigger": false, "messages": [{ "role": "user", "content": "Plan implementation phases, files, and tests for this story." }], "expect": { "writes_files": false }, "must_not": ["rewrite the story backlog"] },
    { "id": "single-bug-ticket", "should_trigger": false, "messages": [{ "role": "user", "content": "File one bug issue for this crash." }], "expect": { "writes_files": false }, "must_not": ["create a backlog"] },
    { "id": "implement-stories", "should_trigger": false, "messages": [{ "role": "user", "content": "Implement the selected stories and run tests." }], "expect": { "writes_files": false }, "must_not": ["only reprioritize them"] }
  ]
}
