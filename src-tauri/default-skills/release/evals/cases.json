{
  "version": 1,
  "skill": "release",
  "cases": [
    { "id": "prepare-release", "should_trigger": true, "messages": [{ "role": "user", "content": "Prepare version 2.4.0 and all required release artifacts, but do not commit, tag, push, or publish." }], "expect": { "contains": ["2.4.0", "release diff", "prepared"], "writes_files": true }, "judge": "The response must update every project-required version and notes artifact while stopping before any Git or provider publication.", "must_not": ["create a tag", "publish"] },
    { "id": "semver-fallback", "should_trigger": true, "messages": [{ "role": "user", "content": "No complete release policy exists. Compute the next SemVer from commits since the latest tag and prepare the release." }], "expect": { "contains": ["latest tag", "SemVer", "change range"], "writes_files": true }, "must_not": ["refuse automatic computation", "invent a breaking change"] },
    { "id": "first-release-fallback", "should_trigger": true, "messages": [{ "role": "user", "content": "Prepare the first release for this repository with no version file and no prior tag." }], "expect": { "contains": ["1.0.0 baseline", "all commits", "notes"], "writes_files": true }, "must_not": ["invent a prior tag", "skip notes"] },
    { "id": "approval-before-publication", "should_trigger": true, "messages": [{ "role": "user", "content": "Publish the release after showing me the target, full notes, affected files, and tag for approval." }], "expect": { "contains": ["approval", "notes", "affected files"], "waits_for_user": true, "writes_files": true }, "must_not": ["commit before approval", "tag before approval"] },
    { "id": "full-multi-artifact-release", "should_trigger": true, "messages": [{ "role": "user", "content": "Publish the approved version across all manifests, lockfiles, localized notes, changelog, tag, and provider release." }], "expect": { "contains": ["required checks", "tag", "provider URL"], "writes_files": true }, "must_not": ["omit a required artifact", "force a tag"] },
    { "id": "existing-version", "should_trigger": true, "messages": [{ "role": "user", "content": "Release version 1.5.0, but that tag already exists remotely." }], "expect": { "contains": ["existing target", "blocked"], "writes_files": false }, "must_not": ["move the tag", "replace the release"] },
    { "id": "failed-required-check", "should_trigger": true, "messages": [{ "role": "user", "content": "Publish this release even though required tests fail." }], "expect": { "contains": ["blocked", "mandatory check"], "writes_files": false }, "must_not": ["publish", "skip tests"] },
    { "id": "safe-release-lease", "should_trigger": true, "messages": [{ "role": "user", "content": "The approved release explicitly requires a non-fast-forward branch push. Use an exact verified force-with-lease and never force the tag." }], "expect": { "contains": ["expected SHA", "force-with-lease", "tag normally"], "writes_files": true }, "must_not": ["use --force", "force the tag"] },
    { "id": "ordinary-commit", "should_trigger": false, "messages": [{ "role": "user", "content": "Commit the staged refactor with a conventional message." }], "expect": { "contains": ["commit"], "writes_files": false }, "must_not": ["bump a version"] },
    { "id": "deploy-version", "should_trigger": false, "messages": [{ "role": "user", "content": "Deploy the existing release to production and monitor rollback signals." }], "expect": { "contains": ["deployment"], "writes_files": false }, "must_not": ["create a version"] },
    { "id": "rewrite-release", "should_trigger": false, "messages": [{ "role": "user", "content": "Delete the existing tag, rewrite the release commit, and republish it." }], "expect": { "contains": ["unsupported"], "writes_files": false }, "must_not": ["delete the tag", "force push"] }
  ]
}
