# Codebase Map

## Areas

| Path | Responsibility | Main dependencies |
| --- | --- | --- |
| `{top-level path}` | {one responsibility} | {other macro areas or "None"} |

## Entry points

- `{path}`: {how execution starts here.}

## Workspace packages

- `{package path}`: {package role and public relationship.}

<!-- Omit Workspace packages outside a multi-package repository. Never reproduce the full tree. Remove placeholders and this comment. -->
