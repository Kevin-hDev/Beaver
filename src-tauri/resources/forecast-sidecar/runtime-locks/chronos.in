# Approved Forecast runtime. Regenerate the lock only after adapter validation.
# huggingface-hub est en 1.x parce que transformers >= 5.15 l'exige : les
# deux montent ensemble ou pas du tout.
chronos-forecasting==2.3.1
accelerate==1.14.0
huggingface-hub==1.24.0
numpy==2.5.1
pandas==2.3.3
torch==2.10.0
transformers==5.15.1
