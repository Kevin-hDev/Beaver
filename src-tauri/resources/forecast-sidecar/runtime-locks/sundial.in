# transformers reste figé : Sundial exécute le code livré avec le modèle
# (trust_remote_code), et generate() casse dès 4.44.2. Il ne bougera qu'une fois
# le modèle amont mis à jour.
torch==2.10.0
transformers==4.40.1
accelerate==0.29.3
