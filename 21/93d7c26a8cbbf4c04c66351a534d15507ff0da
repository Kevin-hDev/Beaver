Review correction follow-up: the frontend SVG security test now reads the tracked src/assets authority instead of the removed private docs path.

Proof: test was red with ENOENT on the docs path during final validation, then passed 2/2 after the correction.
