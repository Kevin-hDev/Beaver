Windows CEF diagnosis checkpoint 2. The public error returned by set_extension_enabled narrows to the confirmed-stop boundary: later host spawn/load failures are projected as per-extension diagnostics instead of returning extensions_host_unavailable. This adds the exact missing short probe: prepared bundled Node, a bound RuntimeHosts channel, active exit monitor, and a voluntary restart stop that must remove the generation and close supervision cleanly. It complements the lower-level restart and two-host probes from 949f0b8f. Local verification: exact monitored restart test passed; E2E workflow contracts 27 passed; cargo fmt --check and git diff --check passed. No production behavior changed.
