Reason: OpenRouter must retain up to 1,000 validated rows deterministically and reject oversized or duplicate snapshots without replacing a healthy runtime registry, while native providers retain their 500-row filtering policy.
RED observed: parser limit 0/1 (500 vs 1000), invalid-row expectation 0/1, duplicate publication 0/1, 501 order/retention 0/1, 1001 preservation 0/1; parent catalog_rows 4/4 red before native-policy corrections; Codex invalid-default guard 0/1 before restoring native filtering.
GREEN targeted: model_catalog 26/26, openai_compat_parsing 16/16, runtime_models 5/5 then parent added exact 1000/1001 guard; kimi_models 4/4, provider_model_registry 30/30, oauth_provider_models 1/1, catalog_limits 1/1, parent catalog_rows 4/4.
Final verification: cargo test -q exit 0 (5307 unit passed + 1 integration passed, 0 failed, 20 pre-existing ignored); cargo clippy --all-targets -- -D warnings exit 0; tsc, contracts, provider boundaries (65 files), fmt, diff check, graphify update all exit 0.
Authority: catalog_limits owns dynamic limits; runtime replacement is atomic for OpenRouter; embedded alias-inclusive limit remains local; OpenAI-compatible limit helpers moved unchanged below the source-size alarm. No live/provider call was made. Known non-failing notices: ts-rs deny_unknown_fields and macOS ld __eh_frame.
