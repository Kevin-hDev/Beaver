Re-re-review closeout. codex_client::supports_tools now states the current Codex protocol contract directly: every served Codex model accepts tools because the catalog wire exposes no per-model tool field. This removes the accidental dependency on generic model-id syntax validation; api_capabilities and both catalogs still use the single authority. The circular catalog test was removed because the real behavior remains locked through api_capabilities. CI workflow job extraction is shared by frontend and CEF contracts and cuts only at a same-indentation job heading. Two underscore-prefixed-job tests were red before extraction was centralized; the nested root-level steps test was found red during self-review before the indentation fix. Full Rust, npm, CEF, release, clippy, rustfmt, ESLint and TypeScript checks passed before commit.
