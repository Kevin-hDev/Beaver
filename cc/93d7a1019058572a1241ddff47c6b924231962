Reviewer: a2d6ee5c changed Silero sha256 directory naming without migration; startup cleanup then deleted the legacy VAD directory. This commit reads the legacy directory when the portable one is absent, preserves it during startup cleanup, treats ASR as unavailable when VAD is missing, and downloads only VAD when repairing an already installed ASR. Checks: 37 Rust voice download tests, 3 frontend controller tests, tsc, lint, cargo fmt, git diff --check. Live app repair requires restarting Beaver Dev and clicking the mic; Windows 0-byte download and localhost webview issue are separate and not resolved here.
