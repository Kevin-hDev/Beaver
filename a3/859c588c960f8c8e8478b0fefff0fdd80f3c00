Intention: établir l'autorité Ollama unique avec ServiceWorkSupervisor<1>, verrou d'opération, génération monotone, états IPC typés et une construction unique dans RuntimeServices; ne basculer aucun ancien consommateur.

Rouge: cd src-tauri && node ../scripts/ci/run-rust-test-filter.mjs --filter services::ollama_manager::manager_tests exécuté avant la production; sortie exacte Rust test filter failed.; code 1, module absent.

Verts réellement lus: le même filtre a passé 13/13 tests (0 échec) et a imprimé Verified 13 filtered Rust test(s).; cargo check code 0; cargo clippy --all-targets -- -D warnings code 0; cargo fmt --check code 0; git diff --check code 0.

Résultat réel: OllamaManager possède l'admission unique, le verrou et l'état avec génération checked_add; abandon, erreur, annulation et panique libèrent l'admission; une génération périmée ne remet pas l'opération suivante à Idle; le dépassement u64 échoue avec OllamaInternal. Les vingt OllamaErrorCode sont stables en kebab-case. Les états, le statut avec progression Option<OllamaProgressStage>, l'endpoint loopback et le résultat de démarrage ont Serialize et TS sous test. RuntimeServices construit une seule instance et lib.rs l'enregistre une seule fois.

Graphify: graphify update . relu après les changements, 32097 nœuds, 64577 relations, 2019 communautés; 73 fichiers sans nœud déjà connus et vue agrégée au-delà de 5000 nœuds.

Limites: aucune installation, démarrage, polling ou arrêt métier n'est migré dans cette tâche; les anciens consommateurs et autorités restent inchangés par intention. Les méthodes de génération de test sont cfg(test). Les fichiers de production sont tous sous 230 lignes.

Recommandation: faire passer les futures opérations par begin_operation et conserver OllamaRuntimeStatus comme seule progression publiée; ne créer aucun second gestionnaire dans les consommateurs.
