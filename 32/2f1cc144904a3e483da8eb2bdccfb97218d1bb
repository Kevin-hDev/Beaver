CI correction: macOS packaged acceptance proved that the Tauri bridge can be ready before React has replayed the persisted update snapshot after reopening the secondary window. The E2E journey now waits for the same four rendered rows it requires on first open before asserting their content. The timeout du test suivant was a cascade from this assertion aborting before cleanup. Proof: failing macOS job 103911613146; 26 E2E CI contract tests passed; npx tsc --noEmit passed; npm run lint -- --quiet passed. Fresh packaged proof is delegated to CI because it requires the packaged macOS WebView harness.
