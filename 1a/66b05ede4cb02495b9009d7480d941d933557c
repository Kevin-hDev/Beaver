Windows test deadline correction found during PR #84 local full-suite validation: SearXNG timeout assertions started before Python fixture discovery, while the actual operation deadline started later inside run_runtime_command. Two timing tests failed before this change; all 16 focused runtime_command_tests passed after it. cargo fmt --check and git diff --check passed. Only tests changed; no runtime deadline was extended.
