Task: add a shared adjustable microphone gain for every voice model. Decision: keep the ASR/VAD contract at 16 kHz because capture already uses the device native rate and resamples once; an intermediate 24 kHz rate adds no source detail. Gain is applied once before both Silero and ASR, with bounded 0/+3/+6/+9 dB levels and +6 dB as the backward-compatible default. Existing version-1 settings deserialize with the new default and retain their other values. Validation: 5 normalization tests, 3 settings tests, Rust/TypeScript contract tests, 87 voice tests with 5 native probes ignored and the external corpus test excluded because VOICE_TEST_CORPUS_DIR is unavailable, TypeScript check, ESLint, cargo fmt, and strict Clippy passed. The panel and dropdown were inspected in dark and light themes; the original dark theme was restored. A real quiet-voice microphone test remains user-hardware validation.
