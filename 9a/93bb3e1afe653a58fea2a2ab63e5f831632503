PR #61: second CI failure investigation and focused Windows corrections (2026-09-08).

Observed CI 34223961488: Windows backend had 5068 passed / 1 failed / 20 ignored; legacy_ui_string_is_retained_but_never_inferred_as_advanced failed with extensions_registry_migration_failed. The historical fixture uses /tmp/legacy-ui, not an absolute Windows path. load_from -> migrate_v1 -> load_v2 -> validate_loaded_records -> validate_local_source correctly rejects it. Normalize only the fixture source to the native tempfile directory, preserving the legacy UI manifest and every assertion. Production path validation and migration behavior are unchanged.

Windows CEF failed strict Clippy because shell_report was compiled on Windows although its sole caller is cfg(unix). Give the helper the same cfg. No allow(dead_code), dependency changes or skipped Windows functionality.

CodeQL alert 110, rust/cleartext-logging, SARIF analysis 1740862999: manually verified false positive and dismissed this individual alert through GitHub with reason false positive. The exact sink is std::Vec<ChatMessage>::insert at fixture_prompt.rs:8, not a logger or file write. Its inserted content is the fixed SYNTHETIC_SYSTEM_PROMPT. ChatMessage::system/new only construct fields. The reported source validate_session_id returns unit or a constant generic error, not a credential. SARIF paths through both api.rs and ollama.rs end at the same in-memory operation; no output sink exists there. Checked the upstream query and CleartextLoggingExtensions at CodeQL commit 1d123a2caa0e4e6256a49d963bfcbd51a01617e8 (model-data log-injection sink). This establishes the false positive in application code, not a claim to have diagnosed the exact upstream model generation error. No CodeQL rule, workflow, or source suppression changed. Regression test synthetic_prompt_replaces_private_system_context_and_preserves_history passed 1/1. The dismissal and reason are visible on alert 110.

Validation actually executed on macOS: storage_migration_tests 12/12; terminal::tests 21/21; fixture prompt test 1/1; cargo clippy --all-targets -- -D warnings exit 0; cargo fmt -- --check exit 0. Separate rustfmt check for included test files initially caught a layout mismatch; formatted those files and reran that exact check successfully. git diff --check passes. Graphify AST update completed (41628 nodes / 92601 edges). Reviewed the complete two-file diff: test-only changes, legacy UI checks retained, helper cfg matches its only caller. Full suites were not rerun locally in this small correction; Windows-native validation and the fresh complete CI remain pending.

Stopped remaining CI job and paused heartbeat before correcting. The prior run had Frontend, Linux backend, Linux WebView E2E, Desktop CEF E2E, Windows extension-host smoke, and dependency audit green; macOS was cancelled, Windows packaged acceptance was not validated. Never treat that run as green. Relaunch via this commit push; no merge authorized. Unrelated website, notices and image/icon work left unstaged.
