Decision: Ollama /api/show details.parameter_size is the authority for prompt tier selection; model-name parsing remains only as a fallback when metadata is unavailable. The exact boundary is compact at or below 25B and detailed above 25B. The Modelfile view receives the Rust-selected tier and exposes only that tier, while global settings keep both choices. The Ollama chat runtime reuses the same metadata resolved for context sizing, avoiding a second API call and UI/runtime drift.

Verification: frontend full suite 461 files / 2426 tests; Rust lib suite 3769 passed / 4 ignored / 0 failed; cargo clippy --all-targets -- -D warnings; cargo fmt --check; npx tsc --noEmit; npm run lint; graphify update . Mutation checks proved the 25B boundary, fixed-tier filtering, and runtime parameter_size stitching each turn red when removed.
