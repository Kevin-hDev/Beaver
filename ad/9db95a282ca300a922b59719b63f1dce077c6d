Fixes Linux CI by compiling the native voice download worker only on macOS and Windows, matching the product boundary. A defensive Linux runner path fails any impossible residual voice download cleanly. Platform-specific imports and voice model re-exports are cfg-gated. Verified locally with cargo fmt, cargo check, cargo clippy --all-targets -- -D warnings, git diff --check, and graphify update .; final Linux proof is the restarted GitHub CI job.
