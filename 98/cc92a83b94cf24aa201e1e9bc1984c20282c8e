CI correction for Backend (Rust), job 105034207198. The regular cargo fmt gate passed, but the dedicated standalone-format gate for changed sources failed first on agent_local_modules_tools.rs and then exposed the same rustfmt drift across the branch Rust files. Applied rustfmt with the exact CI options (--edition 2021 --config skip_children=true) to every changed Rust source; this is mechanical formatting only. Fresh validation: the included-source script passes against the PR merge base, cargo fmt --all -- --check passes, cargo clippy --all-targets -- -D warnings passes, cargo test --lib --no-fail-fast passes (5859 passed, 32 ignored), git diff --check passes, and Graphify was refreshed.
