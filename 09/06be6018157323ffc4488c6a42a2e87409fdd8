CI correction: macOS packaged acceptance showed the WebDriver channel closing while the test switched away from the update window. The journey now waits until the secondary handle is actually removed, switches back to main, and revalidates the Tauri bridge before IPC. Proof: 27 E2E contract tests passed; npx tsc --noEmit passed; npm run lint -- --quiet passed. The native packaged journey is delegated to fresh CI because the local cached bundle predates this head.
