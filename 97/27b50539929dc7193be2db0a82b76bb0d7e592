T12 reviewer note: adds the compact Advanced-settings card immediately after Ollama and a shared SettingsDialog panel for models, capture and memory. Rust projects the packaged catalogue with real sizes/languages/install state. All three ASR models and Silero can be installed/removed; ASR install reuses the shared VAD transfer. Eight persisted settings are wired, custom in-app shortcuts reject existing Beaver shortcuts, and trial uses a separate native destination with real level plus native compute/frontend monotonic delay. Validation: 14 targeted frontend tests and 83 active Rust voice tests passed (2 heavyweight native probes ignored); contracts, tsc, lint, cargo check and strict clippy passed. Linux returns no UI.
