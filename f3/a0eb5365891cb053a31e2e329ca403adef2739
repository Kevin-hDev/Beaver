Review follow-ups N3-m1 and N3-m2. Decision: persist only the bounded positive delta between the generic preparation and the exact provider payload, then apply it only while the same active request, provider, and model still match. This keeps newly appended messages countable without reusing a stale total. Capacity uncertainty now remains CapacityUnverified through snapshot assembly. The generated TypeScript contract includes the persisted delta.
