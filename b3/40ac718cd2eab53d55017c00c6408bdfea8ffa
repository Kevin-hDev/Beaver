Strict verification follow-up: the consumer no longer receives a duplicate cancellation token; it derives it from the single GatewayWorkServices owner. This also restores the seven-argument limit without suppressing Clippy. Evidence: gateway backpressure 3/3; cargo clippy --all-targets -- -D warnings.

Final local verification: cargo fmt --check; cargo clippy --all-targets -- -D warnings; cargo test --quiet --lib --features windows-tests -- --test-threads=1 (3196 passed, 2 ignored); npm run lint; npx tsc --noEmit; npm test (417 files / 1916 Vitest tests plus 50 build-script, 6 CI-script, 29 CEF, 40 E2E-runner and 53 extension-host tests). One pre-existing Windows process-tree assertion was transient in the first full run, then passed 5/5 isolated and in the complete green rerun.
