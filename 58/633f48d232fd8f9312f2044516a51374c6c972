Windows validation follow-up, 2026-09-18; parent 6056eead7b45920459cbb028e1db3b7df25a9e42.

Reason: the acceptance fixture and its fake backend both incorrectly wrapped memory.read in topic. The Rust API and public SDK return the topic directly. First corrected the fake backend and observed 1 pass / 1 failure; then aligned the fixture and imported the SDK types as the single authority. Targeted tests 8/8, API expansion 11/11, explicit TypeScript check exit 0. Graphify update exit 0: 53808 nodes, 111735 edges.

Same native installer SHA256 265011A97957F8F65448650183B192FD4AA417584EF7CEBA1CB95A151BC4221E, no rebuild. Targeted CEF 1/1; its earlier intermittent timeout remains unexplained. Existing Ollama weights reused with a temporary isolated daemon, no download, personal vault, cloud secret or personal conversation copied. Real qwen3.5:latest: H01 model-memory-artifact chain passes with an explicitly essential private manifest and identical fixture body; automatic discovery remains nondeterministic. H02 cancellation and 25024ms deadline: 2/2; same-extension recovery passes, separate neighbor and native Stop remain distinct. H05 two ordered interceptors, repeated individual timeouts, isolated disable and usable neighbor: 1/1. H06 65 concurrent calls, 57 rejected and stable deceptive result: 1/1; multi-host global quota is not proved by this single-host case.

Two native blockers reproduced twice: H03 automation creation rejects the working Ollama model (automation audit: model_unavailable; validation falls through to a catalog that rejects OllamaLocal). H04 loses subagent_extension_owner when inherit_parent_context reloads the child from disk and replaces the in-memory object; the real child continues to completion after its extension is disabled. No product fixes claimed for these findings. H07 native inspection was stopped by the user's physical Escape; not validated. B11 and CL-2/CL-3 remain open.

Report: C:/Users/huynh/projects/Beaver/docs/fonctionnalites/extension/VALIDATION_API_PART_2_WINDOWS_2026-09-18.md. Evidence: this worktree's .validation/2026-09-18. Local commits only; no push, PR, merge, release, CI launch, Codex subagent or Windows restart work. Remote return failed because the source task is not accessible on the local host.
