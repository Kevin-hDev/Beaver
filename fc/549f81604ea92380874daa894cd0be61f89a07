Cause: the synchronous E2E IPC command tried to create the secondary update WebView while Windows WebDriver was synchronously waiting for that command, producing a repeatable circular wait. Fix: expose this test-only command as async so Tauri executes the window creation outside the synchronous IPC response path. Proof: cargo clippy --all-targets --features e2e -- -D warnings passed. Cross-checking the full app with x86_64-pc-windows-gnu is unavailable because cef-dll-sys rejects that triplet; the existing targeted windows-native workflow provides the real packaged proof.
