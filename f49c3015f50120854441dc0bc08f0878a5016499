Objective: address the remaining review finding by preserving diagnostics emitted before the Tauri logging plugin exists, and make the related negative security tests trustworthy.

Decisions:\n- Keep stderr only for the five static, generic pre-logger diagnostics in the Unix bootstrap and standalone updater; do not introduce a second logger or duplicate redaction/rotation logic.\n- Add an updater process regression test proving failures remain visible before Tauri initialization. The test was observed red with log::error! (empty stderr), then green with eprintln!.\n- Replace machine-config-dependent security test skips with explicit temporary allowed/outside roots. The tests no longer read user configuration, touch the home directory, or silently return without asserting.\n- Leave the reviewer observation about Git command log severity unchanged because it was not established as a regression in this scope.

Validation:\n- cargo fmt --all -- --check\n- cargo clippy --all-targets -- -D warnings\n- cargo clippy --all-targets --features e2e -- -D warnings\n- cargo test --all: 3033 passed, 2 ignored; updater integration test: 1 passed\n- cargo test --all --features e2e: 3036 passed, 2 ignored; updater integration test: 1 passed\n- npm test: 411 files / 1875 frontend tests, 40 build-script tests, 6 CEF tests, 6 E2E-runner tests, 51 extension-host tests passed\n- npm run lint, npx tsc --noEmit, npm run contracts:check passed\n- Native npm run test:e2e was attempted but could not start its WebDriver because an already-running Beaver Dev instance with the same single-instance identity caused the E2E app to exit cleanly (code 0). The running user app was not terminated.\n- graphify update .\n- git diff --check
