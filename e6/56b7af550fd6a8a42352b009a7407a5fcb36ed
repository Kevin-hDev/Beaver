CI follow-up: classify reasoning fixture reports/runtime as bounded transient roots, update the reviewed data_dir reference inventory, and explicitly map the historical OAuth vault-key contracts to their current single authority in api_keys_credential_scope_wire.rs. Reason: the preserve-thinking branch added reviewed data roots and moved credential constants without updating the migration contract, while the immutable v1.0.2 baseline attestation must remain unchanged. Verified: persistence migration contracts 9/9, npm lint, git diff --check. Preserve this migration rationale in the final squash commit message; git notes do not survive squash.
