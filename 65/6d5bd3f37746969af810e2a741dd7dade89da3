Décision Kevin du 7 septembre 2026 après review : remplacer le script live demandant une clé en variable d'environnement par le banc d'essai Rust borné de Beaver.

Changements : suppression de scripts/check-provider-usage.mjs (ancien client HTTP indépendant, clé via PROVIDER_API_KEY/OPENAI_API_KEY, sans délai ni plafond de génération). La commande npm test:provider-usage exécute les suites Rust existantes provider_usage et fixture_scope depuis src-tauri afin de respecter sa configuration Cargo. Pas de nouveau client, pas de copie de secret, pas de dépendance ajoutée, pas de changement des clés stockées. Le script supprimé reste récupérable dans Git.

La distinction de preuve est explicite dans scripts/providers/README.md : cette commande est hors ligne, sans accès au coffre utilisateur ni appel facturé ; elle ne remplace pas un verdict live. Les essais réels passent par run_reasoning_fixture_agent_local dans Beaver debug, avec credentials lus en Rust via le coffre, bornes de requêtes et budget préalablement contrôlé. Les métriques d'usage se vérifient séparément ; le seul export de continuité ne prouve pas les compteurs. Aucun appel réel effectué dans ce lot.

Vérification exécutée : npm run test:provider-usage exit 0, 81 tests provider_usage + 6 tests fixture_scope, zéro échec/ignoré. Le premier lancement utilisait --manifest-path depuis la racine ; interrompu (130) puis corrigé pour respecter la configuration Cargo du répertoire src-tauri. git diff --check vert, relecture du diff et des filtres exécutés. Pas de nouvelle suite complète ni de redémarrage de l'application pour ce changement de commande/documentation.

Clarification produit consignée aussi dans les documents locaux ignorés par Git : docs/providers/candidats/ollama-cloud.md, docs/providers/plan-de-tests.md et le plan d'ajout des modèles. L'API native locale Ollama est intégrée, pas l'API Cloud directe authentifiée. Kevin confirme son intégration future, n'a pas de crédit actuellement et n'a jamais demandé d'interdire Ollama Cloud. OLLAMA_NO_CLOUD=1 est une restriction technique héritée du moteur géré, pas une décision utilisateur de confidentialité. Aucun changement de cette restriction ni intégration Cloud opportuniste dans ce lot ; la preuve GLM via relais local reste différée, pas validée.

Graphify AST actualisé, exit 0 : 41531 noeuds, 92441 arêtes, 2121 communautés. Les documents modifiés ne sont pas indexés dans le graphe (vérifié) ; aucun recalcul sémantique revendiqué. Les noms de communautés nécessitent un rafraîchissement sémantique, signalé par Graphify, non effectué ici.

Seuls package.json, l'ancien script supprimé et le nouveau README de procédure sont inclus. Travaux site, notices et assets préservés. Aucun push/PR/fusion/release. Ce lot ne traite pas les autres mineurs de review et n'active aucune nouvelle politique de continuité.
