Decision: make the persisted v6 context_usage record the single authority for a request's preparation, measured input, and output while keeping current preparation separate from the last valid measurement. Legacy v5 context_tokens are removed during migration and are never promoted to provider usage; the exact v5 document is retained in a .v5.bak file. Nested records use camelCase under the existing snake_case session contract, and TypeScript is generated from Rust. Validation: context_usage_record 5 passed; session_migration 30 passed; session_store 38 passed; contracts:check, TypeScript, cargo fmt --check, cargo clippy --all-targets -D warnings, npm lint, and graphify update passed.
