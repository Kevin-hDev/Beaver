MCP Task 3: rmcp 3.4.0 HTTP adapter prototype; not yet selected by registry. macOS/Windows cfg only, Linux retains legacy path. Exact endpoint binding, no SDK auth_header, no redirects, bounded JSON/SSE and catalog pagination, discovery timeout fails closed, NeverRetry and no session reinit, owner cleanup tested during startup and tools/list.

Evidence: cargo test http_sdk --lib -q: 20/20; cargo test -q -- --test-threads=4: library 5927 passed, 35 ignored, CLI 51 passed, macOS integration passed; cargo check -q, cargo fmt --check, git diff --cached --check: exit 0. One earlier full suite run had unrelated private_store fixture failure; isolated rerun and second full suite passed. cargo tree confirms rmcp client + streamable only and no Linux dependency. Linux cross-check blocked by missing native GLib/Pango/Cairo sysroot on this Mac, not claimed green.

Security decision 2026-09-20: Kevin accepts pre-existing reqwest HeaderValue secret-copy zeroization limitation; no additional SDK-held secret copy or destination expansion accepted. Validation details in local docs/fonctionnalites/mcp/validation-implementation-2026-09-20.md. No release or provider integration performed. User Chat Input/i18n edits excluded.
