CI correction for PR #85 at 0f61ccbe. Linux Backend (Rust) failed at strict Clippy: AuthenticatedClient::delete was dead code in Linux production because the only production caller is the macOS/Windows MCP SDK adapter (http_client_request.rs); Linux uses http_legacy. Add cfg(any(macos, windows, test)) to the wrapper, keeping the existing DELETE guard test on all platforms. Local evidence: cargo fmt --all -- --check exit 0; cargo clippy --all-targets -- -D warnings exit 0 on macOS; cargo test -q delete_uses_the_same_url_and_redirect_guards 1 passed; git diff --cached --check exit 0. Linux native CI remains the authority for that target. Pre-existing Chat Input, CodeMirror and i18n edits excluded.
