Task 9 fix round 3; base 566c6fcdfaace5e8f75e06c6b6e51b86430602ed. Scope limited to the final Important cutpoint finding; no production API hooks, Task 10+, Task 14, global suite, CI, push, or merge.

TDD red was read before replacing the test fixture: recovery_tests failed at RemoveUncommittedInstallStaging, SyncParent, before=true because the old wrapper returned Ok(ProgressMade) instead of Err(OllamaStorageUnavailable). The failure proved that SyncParent was not an actual internal boundary.

The test-only RealCutpointFs now executes each durable stage on a real temporary layout instead of delegating an atomic primitive: Write boundaries surround write_all; SyncFile boundaries surround file.sync_all; Publish boundaries surround tmp-to-final fs::rename; Rename boundaries surround layout fs::rename; SyncParent boundaries surround parent File::sync_all; Remove boundaries surround remove_file/remove_dir_all. It preserves real tmp/final and layout states after each injected error and has no production API or hook change.

The real recovery table invokes public recover for every destructive scenario: RemoveUncommittedInstallStaging, both CommitFreshInstall sources, both legacy restore sources, ResumeTargetValidation, PersistRollbackCleanupPending, both failed transitions, RemoveCompletedLegacyJournal, and AdoptLegacyActive. It injects before/after for every applicable Write, SyncFile, Publish, Rename, SyncParent, and Remove point, clears the fault, and requires two consecutive equal real layout snapshots.

Verification: recovery_tests 14 passed/0 failed; durable_fs_tests 19 passed/0 failed; cleanup_tests 4 passed/0 failed; rollback_tests 5 passed/0 failed; cargo check passed; cargo clippy --all-targets -- -D warnings passed; cargo fmt --all -- --check passed; git diff --check passed; graphify update . passed with 33,375 nodes, 68,076 edges, 2,031 communities and the known 73 zero-node warnings. Production remains unchanged and under 230 lines; path_identity_windows_handles.rs is 104 lines, recovery.rs 221, durable_fs_windows.rs 218. Native Windows execution remains unavailable on macOS and is not claimed. Commit 4f4fc25ec7b1ba1f23c58ef3cf0760daa0c2a4a7.
