Jalon 3 Ollama — preuve de cible reconstruite après renommage durable

Cause racine
- La cible était préparée sous `ollama-bundle-update-staging`.
- Après le renommage durable vers `ollama-bundle`, la sonde recevait encore le PreparedBundle lié à l’ancien chemin disparu.
- Le contrôle strict d’identité de chemin rejetait donc correctement l’archive officielle avec `ollama-bundle-invalid`, puis la transaction restaurait l’ancien bundle.

Correction
- `reinspect_active` vérifie désormais au chemin actif l’identité canonique, le reçu exact et le SHA-256 réel, puis retourne un nouveau PreparedBundle lié à cette destination.
- Le backend de mise à jour transmet cette preuve reconstruite à la sonde.
- Le contrôle strict chemin + identité n’est pas relâché : la preuve change d’autorité au moment précis où le renommage durable change l’emplacement autoritaire.

TDD
- Rouge : le nouveau test ne compilait pas car `reinspect_active` retournait `()` ; aucune preuve au chemin actif n’était disponible.
- Vert : `reinspection_rebinds_the_prepared_bundle_to_its_committed_path` passe et exige le chemin actif canonique, l’exécutable sous ce chemin et l’empreinte inchangée.

Validation exécutée
- Test exact : 1 passé, 0 échec.
- Filtre officiel `services::ollama_manager` : 314 passés, 0 échec, 1 commande développeur ignorée.
- `cargo clippy --all-targets -- -D warnings` : vert.
- `cargo fmt --all -- --check` : vert après formatage mécanique.
- `git diff --check` : vert.
- Fichiers de production touchés : 96 et 181 lignes, sous la limite de 230.
- `graphify update .` : terminé, graphe à jour.

Suite requise
- Reconstruire et installer l’application complète, puis rejouer la mise à niveau native macOS 0.32.1 vers 0.32.13.
- Ne déclarer la preuve native verte qu’après validation du modèle conservé, du redémarrage hors ligne et de la fermeture coordonnée.

Preuve native macOS exécutée sur ce commit
- Build complet : `CLGO_CEF_ALLOW_ADHOC_SIGNING=1 npm run tauri build` terminé ; `.app` et `.dmg` produits puis application complète installée.
- État de départ réel : bundle actif déclaré 0.32.1, modèle Gemma 3 déjà présent, aucune transaction active.
- Mise à jour UI réelle : téléchargement/extraction de l’archive officielle, puis convergence vers VERSION 0.32.13 sans journal, staging, backup ni rebut résiduel.
- Archive macOS officielle : SHA-256 `71efd44f3b5f2019f42bae17ae58eb3de8bd25ce3ca3bc89aea58e53e5d091d1`.
- Reçu actif : version 0.32.13, exécutable SHA-256 `dee2ad206ec73dc19d4eeaf13f35ff54022977242ed38d53dda65da4c70a9c95`.
- Modèles : les six entrées et leurs empreintes sont strictement identiques à `macos-before.json` ; 3 338 802 660 octets conservés.
- Gemma 3 a répondu exactement `OK` via le sidecar possédé 0.32.13 après la mise à jour.
- Redémarrage avec HTTP_PROXY, HTTPS_PROXY et ALL_PROXY dirigés vers `127.0.0.1:9` : le sidecar local a démarré et Gemma 3 a de nouveau répondu exactement `OK`, sans re-téléchargement.
- Fermeture coordonnée via le tray exécutée deux fois : nettoyages observés en 89,9 ms puis 239,3 ms ; aucun processus Beaver/Ollama, reçu de processus, gate ou artefact transactionnel restant.
- Preuve écrite locale : `docs/superpowers/validation/native-upgrade-proofs/macos-recovered-final.json`, 9 979 octets, SHA-256 `3e9af3f5453459d221f6bbe0ae148f40e8bb72b56f16fa81188b8be337c35cf1`.

Portée
- La preuve macOS de mise à niveau, reprise après interruption, conservation du modèle, redémarrage hors ligne et fermeture coordonnée est verte.
- Les preuves natives Linux et Windows restent à exécuter sur leurs machines respectives ; elles ne sont pas déclarées vertes ici.
