Review corrections after 2bc089e2

Resolved majors: multiline instructions remain valid while forbidden controls stay rejected; completed one-shot definitions accept an explicit disabled replacement schedule; unreadable config no longer blocks startup and is reported as migration unavailable without rewriting bytes; global pause preserves the scheduler checkpoint so crossed deadlines are handled on resume; schedule calculation moved under automations to remove the production scheduler/automations cycle.

Resolved remaining findings: past one-shot dates are rejected; identical after-completion updates keep their anchor; pending work rechecks within 60 seconds; terminal history publication retries without restart; missed-grace has one authority; external-instruction ownership scenario is covered; timezone migration uses the existing bounded CustomSelect with an accessible filter; initial loading and next-execution labels are explicit; automatic session titles are language-neutral; the mutating migration command is named reconcile; stale allowances and dead wrappers were removed.

Validation: cargo test --no-fail-fast => 5435 passed, 0 failed, 21 ignored; npm test => 607 files and 3334 Vitest tests passed, all script suites passed, exit 0; cargo clippy --all-targets -- -D warnings => exit 0; npx tsc --noEmit => exit 0; npm run lint => exit 0; cargo fmt --all -- --check and git diff --check => exit 0; graphify update . => exit 0. Native app: the user confirmed npm run tauri dev launches normally. The separate isolated visual attempt was not counted because an inconsistent manual CEF test profile caused helper supervision failure before UI startup.
