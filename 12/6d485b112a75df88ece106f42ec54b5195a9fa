Suite a review-corrections-2026-09-20.md : m3 ferme par subtle::ConstantTimeEq et Choice, sans changer la longueur fixe, la verification de format ni le zeroize. C1 : un identifiant hors catalogue avec endpoint invalide provoque une erreur de chargement explicite au lieu de disparaitre silencieusement ; ce cas main-edite ne devient pas reparable dans UI. C2 : lecteur unique config_read pour demarrage, lecture stricte et reparation ; borne 256 KiB et rejet des identifiants dupliques communs, ecriture stricte refuse aussi les doublons. Tolerance reservee aux endpoints invalides de connecteurs connus du catalogue.

TDD : 3 nouveaux tests ont echoue avant correctif sur C1, doublons et taille ; 16/16 config_persistence ensuite. OAuth : premier filtre de test a lance 0 test, corrige puis 3/3. Suite finale cargo test -q -- --test-threads=4 sortie 0 : 5979 lib verts, 35 ignores, 51 CLI et integrations vertes. npm test sortie 0 : 641 fichiers, 3500 tests, sous-suites incluses. cargo clippy --all-targets -- -D warnings, cargo fmt --all -- --check, npm run lint, npx tsc --noEmit et git diff --check sortie 0.

Graphify AST update a retire 257 noeuds documentaires encore presents ; restauration ciblee depuis copie avant mise a jour, avec nouvelles relations de code conservees. Graphe final : 48105 noeuds, 108887 liens, 4 hyperedges, 0 lien casse, aucun document historique perdu, 34 noeuds de review MCP sans orphelin. Livraison toujours bloquee par les validations reelles de la tache 7 : fournisseur, interface, builds Linux/Windows natifs. Modifications Chat Input et selectionDeleted de Kevin laissees hors commit.
