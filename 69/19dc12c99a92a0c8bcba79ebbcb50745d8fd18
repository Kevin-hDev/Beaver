CI correction review note

Observed failure:
- Windows packaged acceptance failed twice while loading healthy fixtures.
- The recovery marker moved between activate and register, while the same prepared Node/Rust host tests passed.
- The host reader treats a loading-journal write failure as fatal; Windows private-store replacement previously made one unretired MoveFileExW call.

Decision and reason:
- Centralize the existing 2 s / 50 ms bounded Windows sharing retry and reuse it for both Ollama durable files and the private store.
- Retry only ERROR_SHARING_VIOLATION and ERROR_LOCK_VIOLATION. ERROR_ACCESS_DENIED remains fail-closed because it may be a real permission failure.
- Keep the loading journal atomic and durable; no fallback write and no weakening of recovery guarantees.
- Add a generic bounded trace plus safe E2E activation state so a remaining failure identifies its public code without paths, stack traces, or secrets.

Validation executed:
- cargo fmt --check: passed
- cargo clippy --all-targets --features windows-tests -- -D warnings: passed on macOS host profile
- cargo test services::windows_fs_retry --lib: 2 passed
- cargo test services::private_store --lib: 25 passed
- cargo test services::ollama_manager::durable_fs_tests --lib: 26 passed
- npx tsc --noEmit: passed
- node --test scripts/e2e/native-diagnostics-artifact.test.mjs scripts/e2e/e2e-ci-contracts.test.mjs: 25 passed
- npm run lint: passed
- git diff --check: passed
- graphify update .: completed

Known validation boundary:
- A local x86_64-pc-windows-msvc cargo check could not complete because this macOS host has no Windows SDK headers. The native Windows compile and packaged acceptance are intentionally delegated to CI.
