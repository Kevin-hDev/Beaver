Review seam checks 1/2: confirmed Linux comm truncates the gated Ollama name to '.beaver-gated-' plus one PID digit. The owning Unix gate module now accepts only that exact 15-byte truncated form on Linux; full names remain parsed by the existing owner authority. Also documented that CLI staging paths are direct root children. Red-green proof: the new matcher test first failed, then support tests passed 8/8 and the CLI matcher test passed. Files remain below 230 lines; fmt and diff checks passed.
