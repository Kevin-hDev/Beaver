Root cause: live OAuth metadata for Vercel, Figma and Apify advertises exact hosts absent from trusted_oauth. Added only api.vercel.com, api.figma.com, console.apify.com and console-backend.apify.com; no wildcard or redirect relaxation. The OAuth dialog now gives a localized safe explanation for the exact untrusted-endpoint error.

Proof: the metadata regression test failed on Vercel before the change and passed afterward; the dialog test failed on the generic message before the change and passed afterward. npm test exit 0 (642 Vitest files, 3502 tests, plus script suites); cargo test --lib --quiet exit 0 (5980 passed, 35 ignored); cargo clippy --all-targets -- -D warnings, cargo fmt --all --check, npm run lint, npx tsc --noEmit and git diff --cached --check exit 0. The first full Rust run had one unrelated intermittent private_store fixture error; its exact rerun and two later full runs passed. graphify update . exit 0.

Limitations: no real provider-account authorization or browser callback tested. Vercel and Figma document client admission requirements; this local fix does not grant provider approval. Existing Chat Input, CodeMirror and selectionDeleted translation edits were excluded from the commit.
