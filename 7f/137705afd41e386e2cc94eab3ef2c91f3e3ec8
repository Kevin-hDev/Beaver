Review corrections:\n- Removed orphan purging from launch scripts; the Rust installer now adopts and marks the current run with its PID before centrally purging validated inactive runs.\n- Active markers are opened without following links; malformed or unverifiable markers fail closed and are preserved.\n- Retry requests keep the failed row until the main application accepts the new start, then backend dismissal is reflected through update-operation-dismissed.\n- First-run Ollama failures are not offered as retryable because UpdateProvider is not mounted there.\n\nReason: never delete concurrent installation work and never hide a failed operation before its retry is accepted.\n\nValidation:\n- npm test: 620 files, 3462 tests passed\n- npm run test:installer: 8 UI, 48 Rust passed (2 ignored), 3 boundary, 2 SVG passed\n- cd src-tauri && cargo test: 5626 library tests passed (22 ignored), 51 CLI tests passed, auxiliary suites passed\n- npm run lint; npx tsc --noEmit\n- cargo fmt --check and cargo clippy --all-targets -- -D warnings in both Rust workspaces\n- bash -n install.sh; git diff --check\n- graphify update . completed\n- Native Windows compilation remains delegated to CI: local cross-check cannot run without the Windows SDK/llvm-rc.
