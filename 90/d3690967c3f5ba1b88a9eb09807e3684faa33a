CI follow-up: compile note_path_for_test only for Unix tests, matching its sole symlink-test caller. Reason: Windows strict Clippy correctly rejected the otherwise unused test helper; production behavior is unchanged. Verified locally: cargo fmt --check, clippy --all-targets -D warnings, fixture tool tests 2/2. Preserve the platform-scope rationale in the final squash commit message; git notes do not survive squash.
