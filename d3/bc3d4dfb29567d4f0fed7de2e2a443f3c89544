Release run 32537472741 proved macOS and Linux green, while Windows failed before entering the SearXNG test with STATUS_ORDINAL_NOT_FOUND after the Tauri release build staged CEF DLLs. The release smoke test now selects the existing windows-tests feature only for the Windows matrix entry; that profile is the repository authority for executable Rust tests that must not load libcef.dll. The workflow contract locks the per-OS matrix values. Verified locally: the ignored release wheelhouse test starts and passes under --features windows-tests (1/1, 5.19s), npm run test:release-workflow passes 39/39, cargo fmt --check and git diff --check pass, graphify update reports no topology change.
