CI correction: Windows packaged acceptance reproduced twice that the update-window security test blocked while deleting an API key during setup. The E2E runtime starts with an empty ephemeral vault, so this write was unnecessary; setup now asserts the empty precondition read-only, then verifies it remains unchanged after the denied secondary-window command. Proof: Windows jobs 103961414208 and 103965070596 failed at the same delete_api_key call; 26 E2E CI contract tests, npx tsc --noEmit and npm run lint -- --quiet passed locally. Native proof is run through the existing targeted Windows diagnostic acceptance before updating the PR head.

Follow-up: targeted Windows run 34842037080 proved has_api_key completed and e2e_seed_update_operation timed out next. This refines the diagnosis: the removed vault write was unnecessary but not the deadlock cause.
