Fixes the next CI boundary: VoiceDownloadFailure and download_percentage are compiled only where the voice download worker exists, and the Windows native file flush helper accidentally removed by the voice installation commit is restored with its original GENERIC_WRITE/FlushFileBuffers contract. Adds a source-level regression test runnable on macOS. Verified with cargo fmt --check, cargo check, cargo clippy --all-targets -- -D warnings, the exact windows_sync_file_keeps_its_native_flush_implementation test (1/1), git diff --check, and graphify update .; Linux and Windows CI are restarted for native proof.
