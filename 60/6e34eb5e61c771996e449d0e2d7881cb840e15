MCP Task 4: macOS/Windows HTTP now uses the rmcp adapter; Linux retains the prior HTTP path. McpToolCatalog carries TTL; cache has one global account generation, a 32-entry bound, exact expiration and no-cache deletion. Probes do not publish. Identity mutations invalidate before writes under one lock; same-account OAuth refresh writes only if generation is unchanged. Agent and extension share a pre-send gate; HTTP and stdio check again after initialization. Extension timeout/revocation reports an unconfirmed result.

Evidence on macOS: cargo fmt --check exit 0; cargo clippy --all-targets -- -D warnings exit 0; cargo test -q -- --test-threads=4 exit 0, library 5939 passed/35 ignored, CLI 51 passed, macOS integrations passed; git diff --cached --check exit 0. Targeted tests cover stale cache publication, 33rd entry eviction, exact TTL, stale business send, OAuth refresh versus identity change, one business call during same-account refresh, extension permission refusal and uncertain-result mapping. Tests use loopback/in-memory secrets, not the user vault.

Limits for reviewer: no Windows or Linux compilation on this Mac; cargo tree shows no rmcp dependency on Linux. No real provider or UI walkthrough yet; Task 7 requires it before delivery. Global generation intentionally invalidates other connector caches (max 32), refresh does not. Local validation report: docs/fonctionnalites/mcp/validation-implementation-2026-09-20.md. Existing Chat Input/i18n edits excluded.
