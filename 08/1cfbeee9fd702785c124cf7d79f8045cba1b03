Purpose: preserve the product rule that Ollama memory sizing is advisory only. The new component test exercises the real ModelInstallButton and useModelDownloads flow with a negative check_model_fits_vram result, then requires both the warning toast and the visible running-download cancel state. Mutation proof: adding return immediately after the warning makes the test fail because the download never starts. Copy: all seven locales now use generic machine memory, valid for unified RAM on macOS and VRAM on Windows/Linux, and explicitly state that the download continues. Verification: targeted Ollama/download suite 12 files and 61 tests, full npm test 463 Vitest files and 2439 tests plus build/CI/CEF/E2E-runner/extension-host suites, TypeScript, lint, diff check, and graphify update all passed.
