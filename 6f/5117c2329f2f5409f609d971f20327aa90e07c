Review corrections for docs/superpowers/plans/2026-09-09-review-commits-fiabilisation-providers.md\n\nScope and decisions:\n- Q1: AdmissionRollback now owns the assistant identifier and exposes the single accept_execution_event boundary. Interactive chat, gateway channels, and scheduled wakeups all use it, so an execution accepted by the UI retains the admitted user message on later failure.\n- S1: LiteLLM refresh rejection is now observable through one structured, bounded log containing only a stable reason and consumed-entry count. Invalid refreshes still preserve the previous registry and cache.\n- R1: Persisted session_inconsistent and model_invalid failures resolve to actionable text in all seven supported languages.\n- R2: API catalog failures are preserved per provider and rendered in the model selector. Only known stable codes reach translation; unknown/raw rejection details collapse to model_catalog_unavailable.\n- T1: xAI OAuth catalog Retry-After parsing now delegates to the same bounded parser as provider streams.\n- O2 product decision: an OpenRouter model with future or malformed non-null reasoning metadata remains selectable, but exposes only ProviderDefault/auto. Beaver sends no invented effort and emits one aggregate degradation trace per catalog parse.\n\nTDD and review evidence:\n- Red tests were observed for the missing shared admission boundary, silent LiteLLM rejection contract, untranslated persisted errors, invisible catalog failures, unbounded xAI Retry-After parsing, and unknown OpenRouter reasoning behavior.\n- The first complete Rust run exposed two historical tests enforcing the superseded O2 hide-model policy. They were updated to prove the explicit keep-model/provider-default decision rather than weakened.\n- cargo test --lib -- --test-threads=4: 5391 passed, 0 failed, 21 ignored.\n- npm test -- --run: Vitest 3324 passed across 604 files; remaining scripted CEF, CI, extension-host, E2E, reasoning fixture, and contract checks completed with exit 0.\n- cargo fmt --check: passed.\n- cargo clippy --all-targets -- -D warnings: passed.\n- npx tsc --noEmit: passed.\n- npm run lint: passed; provider routing boundary count remains 65.\n- Targeted Rust suites passed: agent_chat_run 7, LiteLLM catalog 20, OpenAI-compatible parsing 21, xAI catalog 6.\n- Targeted frontend suites passed: 25.\n- git diff --check: passed.\n- graphify update . completed: 42309 nodes, 95988 edges, 2075 communities.\n\nNo paid/provider live request was made and Beaver was not restarted. The model-selector catalog-failure state is covered by an automated rendering test, not a manual visual run.
