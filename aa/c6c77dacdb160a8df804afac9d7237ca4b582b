Review correction:
- Re-clamp the update window position after applying its requested height.
- Preserve in-bounds positions and move only the overflowing axis inside the current monitor work area.

Reason: the window was centered at its initial 96 px height, then grew without moving, which could place its lower actions outside the screen.

Red proof:
- targeted Rust source-order test failed because resize had no post-size set_position call.

Validation:
- two targeted update-progress resize tests passed
- cargo fmt --check passed
- cargo clippy --all-targets -D warnings passed
- git diff --check
- graphify update . completed.

Full regression after all four review corrections:
- npm run test:installer: 8 UI tests, 49 Rust tests passed with 2 ignored, 6 boundary tests passed
- npm test: 620 frontend files and 3467 tests passed; all build, CI, filesystem, CEF, E2E runner, extension host, reasoning, and contract suites passed
