Final minor remediation for REVUE_CORRECTIFS_EXTENSIONS_2026-09-02.md.

B1-01 closed at the ownership boundary: retain_failed now creates a requested-exit signal only after the failed pre-bind process and its temporary directory are owned by RuntimeHosts, and the caller publishes it after releasing the registry lock. The regression test reproduces the real race by consuming the original unexpected reader notice before retention, then proves the synthetic notice reaps the retained process and directory.

B2-01 closed: when the last attempted extension was not applied, completion restores a distinct valid interruption marker preserved before the sync instead of leaving the failed neighbor marker. The new test reproduces original X, successful neighbor Y, failed-last Z and verifies that X remains recoverable.

B3-01 and B3-02 closed through one authority: contract.json now owns hostStates; Rust HostState, the frontend constant and type, Node validation and SDK declarations are generated or read from that contract. EMPTY_EXTENSION_HOST now uses the generated API version. The enum renderer rejects Rust-variant collisions. A2-07 remains intentionally ordered and now carries an inline rationale: publishing the disabled and untrusted replacement closes routing before the previous channel is revoked.

Self-review: no new user-visible text was introduced. All changed production files remain at or below 230 lines. Generated artifacts match the contract. The failed-retention signal is sent without holding RuntimeHosts, so the monitor cannot deadlock on the registry lock; a closed monitor keeps the process retained for the application exit path, preserving fail-closed behavior.

Validation: npm run lint PASS; npx tsc --noEmit PASS; npm test PASS (2867 Vitest, 56 build-script, 16 CI-script, 45 CEF plus 1 Windows-only skip, 70 E2E-runner, 57 extension-host, 10 reasoning fixture tests); cargo fmt --check PASS; cargo check PASS; cargo clippy --all-targets -- -D warnings PASS; cargo test PASS (4727 library tests, 10 ignored, plus 1 integration test); cargo check --lib --target x86_64-pc-windows-gnu --features windows-tests PASS. graphify update . completed. The macOS linker emitted the pre-existing __eh_frame warning; the Windows cross-library check emitted its pre-existing CEF cfg and dead-code warnings.

Native visual validation used the exact worktree build via npm run tauri dev and Beaver Dev, not the installed application. The Extensions tabs showed Plugins, Extensions and Host only; Applications externes was absent. The Host panel was inspected in dark and light themes with its validated Starting state, version fields, actions and recovery section readable and stable. The original dark theme was restored before closing Beaver Dev, and port 5173 was confirmed free.
