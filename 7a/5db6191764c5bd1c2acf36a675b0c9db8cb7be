Provider reliability follow-up, 2026-09-09.

Root cause: Chat Completions ignored all normal finish_reason values. A DONE marker therefore admitted empty or truncated answers as successful, including the observed Muse 1.1 test with 512 output tokens. The old live trace did not retain its reason: exhaustion is a hypothesis for that trace, not reconstructed evidence.

Decision: one completion classifier shared by interactive and silent Chat Completions readers. Preserve only known normalized reasons; retain partial text and billed usage with a terminal error. Discard tools before emitting calls on length/content_filter. The API loop checkpoints partial content, updates context counters and fails before tools, plan repair, compression or follow-up. Do not acknowledge pending subagent reports for such a failure. Silent summaries fail after recording request metrics. Three actionable error codes have all seven translations. No automatic paid retry, model restriction, account change or production output-limit adjustment. Native Anthropic/Responses/Codex/Ollama completion contracts remain unchanged. StreamOutcome Completed describes completed transport and may carry completion_error; consumers and metrics explicitly check it.

Accounting: the existing request-journal validator requires usage_complete=false for failed requests, even when usage exists. Disk round-trip tests cover failed status, preserved finish reason and exact counters for interactive and silent consumers. The session ledger is updated before the loop rejects the failed result. ApiRequestParams was moved into the existing request-types module to keep the orchestrator below 230 lines.

Verification observed: regression test first failed None vs Some(length), then passed. New SSE, partial persistence, diagnostics, metrics and API report-ack tests pass. Full current Rust library binary: 5389 passed, 0 failed, 21 ignored using --quiet --test-threads=4 (67.47s). Earlier full runs exposed an obsolete source-string assertion (replaced on API with real request behavior), an incorrectly framed test fixture (fixed), and an intermittent old Ollama owned_drop_keeps_receipt_and_slot_when_reap_fails admission failure. That Ollama test passes alone and in the final full run; no claim that its intermittent issue is fixed. Native test data is isolated by paths::data_dir under cfg(test).

Commands: cargo test --lib and targeted tests; final entire freshly built target/debug/deps/cl_go_dash_lib-3b95be52aefb6226 --quiet --test-threads=4 executed directly to avoid the concurrent bundle-build Cargo lock. CARGO_INCREMENTAL=0 CARGO_PROFILE_DEV_DEBUG=0 CARGO_PROFILE_TEST_DEBUG=0 used for local Cargo commands. cargo clippy --all-targets -- -D warnings, cargo fmt --all --check, npx tsc --noEmit, npm run lint (65 routing-boundary files), eight agent-error-codes Vitest tests and git diff --check passed. Full frontend suite was not rerun in this lot. Graph AST updated: 42292 nodes, 95940 edges.

Cleanup recovery: restored verified CEF source and npm ci dependencies after deleted build caches. No dependency manifest/lock edits. npm audit separately reports pre-existing js-yaml 4.3.1 GHSA-2883-xcg3-v3hh, fix 4.3.2; not fixed by this lot. Cold bundle rebuild and fresh Muse/Nemotron live verification still pending at commit time. Do not claim these models fully validated from offline tests.

Detailed local journal: /Users/kevinh/Projects/Beaver/docs/providers/openrouter/validation-correctifs-2026-09-09.md. Docs are intentionally outside the isolated worktree and ignored by Git. No push or merge performed.
