CI correction:\n- Gated macOS/Windows-only update-window imports, constants, saved position state, and positioning helpers out of Linux builds.\n- Linux behavior remains unchanged: the new update window stays invisible as required.\n\nReason: Linux maintenance builds compile with -D warnings, so platform-specific UI code must not exist in the Linux compilation unit.\n\nValidation:\n- cargo fmt --check\n- cargo clippy --all-targets -- -D warnings (macOS): passed\n- cargo test services::update_progress: 7 passed, 1 ignored\n- git diff --check\n- graphify update . executed\n- Linux cross-clippy was attempted locally but stopped in third-party GTK build scripts because the macOS host has no Linux pkg-config sysroot; native Linux CI is the authority.
