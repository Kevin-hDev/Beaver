Task 10 follow-up: interrupted archive staging is now owned by OllamaPaths and recovered by the existing RecoveryExecutor. No Task 11+, global suite, CI, push, or merge was performed.

TDD red: before this change, interrupted_archive_staging_is_moved_to_rebut_before_retry observed Ok(Ready) instead of Ok(ProgressMade), proving the central snapshot/recovery path ignored a leftover archive staging directory.

Implementation: OllamaPaths is the sole authority for archive_staging and archive_failed. The central snapshot classifies both paths. Recovery converges in bounded passes: pass 1 validates the staging directory with validate_trash and durably renames it to archive_failed; pass 2 validates and removes the rebut with remove_trash/remove_tree_verified; pass 3 reaches Ready. Ambiguous coexistence, symlink/non-directory, and unknown metadata fail closed without deleting the path or its outside target. transaction_locations includes both paths so model-store conflict checks cannot overlook them. With no active bundle, recovery resolves and freezes the model directory through the existing spawn-profile path authority; failure to prove it defers recovery.

Proof: recovery_tests 17/17 (including two-pass convergence, a real subsequent install that reaches OllamaBundleInvalid rather than being blocked by stale recovery, ambiguous coexistence, and symlink preservation); install_tests 9/9; spawn_profile_tests 19/19; document_tests 10/10. cargo check --lib --no-default-features, cargo clippy --all-targets --no-default-features -- -D warnings, cargo fmt --all -- --check, git diff --check, and graphify update . all passed. Windows was not executed on this macOS host and is not claimed green. The local task-10 report was updated but no documentation file was committed.
