Decision: session identifiers are omitted from migration and crash-recovery warnings because operational logs only need the failure category and session IDs may identify private conversation data. The release-build-only unused parameter is marked explicitly without changing debug fixture behavior. Validation: Rust 1.98.1 formatting, strict clippy, both malformed context migration regression tests, git diff check, and graphify update passed.
