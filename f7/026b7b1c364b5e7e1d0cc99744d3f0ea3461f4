Full-suite correction — transient compression overhead baseline.

Decision: provider overhead is the difference between two complete request payload counts. The snapshot stores only system and developer messages after computing that full baseline; otherwise ordinary conversation history is counted twice and valid automatic compression is rejected.
