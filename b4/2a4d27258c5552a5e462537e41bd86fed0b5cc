Task 10 J3 Beaver — base a9944bdc36a06f91d7ea7bab63012058bc8f769b. Périmètre strict : première installation Ollama atomique uniquement ; aucune mise à jour Task 11+, aucune adoption Task 14, aucune suite globale/CI, aucun push/merge.

TDD : avant production, le filtre services::ollama_manager::install_tests a retourné exactement Rust test filter failed. Les modules download, extract et release source ont également été testés avant leur logique. PreparedBundle de Task 7 reste l'unique type de préparation.

Implémentation : release_source/release_fetch possède le manifeste HTTPS GitHub allowlisté, la version et le checksum ; download borne deux archives, les tailles et le flux, écrit en create_new avec flush/sync et vérifie SHA-256 constant-time ; extract/extract_archive/extract_zip_validate extrait TAR/ZIP réels avec staging vide, traversal/absolus, symlink, hardlink, doublons et flux surdimensionné refusés ; bundle_receipt/bundle_install possède VERSION, reçu durable, préparation et réinspection ; install réalise download/verify/extract/remove, probe owned, commit unique install-staging vers active et réinspection ; les six commandes demandées délèguent à ces autorités ; install_facade expose l'admission OllamaManager.

Preuves comportementales finales : download_tests 9/9 avec fixture HTTP local, flux exact durable, redirection hors allowlist, HTTPS, checksum exact, taille mismatch et limite deux archives ; extract_tests 8/8 avec TAR/ZIP réels, traversal, absolu, symlink, hardlink, doublon et membre TAR >4 Gio ; release_source_tests 3/3 ; install_tests 8/8 avec ordre des frontières, staging non publié sur archive invalide, cohérence VERSION/reçu, probe réel, publication et réinspection. Toutes les assertions ont été lues vertes.

Contrôles finaux lus verts : cargo check ; cargo clippy --all-targets -- -D warnings ; cargo fmt --all -- --check ; git diff --check ; graphify update . avec 33580 nœuds, 68609 relations et 1981 communautés, avertissement connu de 73 fichiers sans nœud. Production Task 10 sous 230 lignes : extract 118, extract_archive 214, extract_zip_validate 90, install 222, bundle_receipt 136, bundle_install 105, download 197, release_source 194, release_fetch 56 ; commandes et façade également sous 230.

Limite factuelle : validation native Windows non exécutée sur macOS arm64 et non présentée comme verte. Rapport local et ledger local mis à jour ; ils restent ignorés comme les tâches précédentes. Commit exact : b42a4d27 feat(ollama): commit first install atomically.
