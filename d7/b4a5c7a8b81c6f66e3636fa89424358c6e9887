Decision: persist context preparation, measured input, and output under the exact request, turn, and attempt identity so late events from request A cannot overwrite request B. Provider input is normalized once per API contract: Anthropic adds uncached, cache-read, and cache-write input, while OpenAI-compatible routes keep their already-inclusive input. A response without measured input preserves the previous measurement and its context limit; output remains independently attributable. Startup converts orphaned in-flight preparations to interrupted, and mutations stale only the preparation. Validation: conversation_journal 10, context_usage_record 6, context_usage_runtime 6, context_usage_startup 1, context_input_tokens 1, anthropic 71, payload_too_large 2, payload_reduction_retry 1, thinking_retry 11, mascot_outcome 1, stream_consume 9, stream_silent_consume 6, stream_accumulator 10, types_stream 9, frontend stream tests 34; TypeScript, ESLint/provider boundaries, cargo fmt, Clippy, git diff check, and graphify update passed.
