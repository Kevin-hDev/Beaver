CI follow-up: canonicalize the catalog bundle path before matching the temporary context-fixture skill. Reason: Windows canonical paths carry platform normalization (including the extended path form), so comparing one canonical path with one raw ReadDir path made six context tests fail before exercising production. Production behavior is unchanged. Verified locally: context suite 7/7, cargo fmt --check, clippy --all-targets -D warnings. Preserve this platform-test rationale in the final squash commit message; git notes do not survive squash.
