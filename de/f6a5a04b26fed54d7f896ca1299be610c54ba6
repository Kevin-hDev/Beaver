Review context — P0.3 effect-based permissions

Scope: generates ExtensionEffect from the R0 executable contract; makes the Rust effect policy authoritative for confirmation, parallel reads, Plan mode and session caching; binds grants to session/extension/tool; propagates bounded redacted permission metadata; adds the seven translated labels; gives Explorer/Coder read-only extension tools and Coder controlled sensitive tools; rejects duplicate canonical tool ownership; keeps chat mode closed to extension tools; declares only the effects of the existing ten Office plugin tools.

Controller review corrections before commit: (1) restored effect as required in the TypeScript SDK while Rust remains tolerant for legacy or malformed runtime data; (2) added Rust-derived allowSession so process, secret and unknown never display a persistence action the backend refuses; (3) clear all extension grants on global Host unavailability/recovery while preserving native grants; (4) centralized the frontend request shape in AgentPermissionRequest; (5) replaced a parallel-racy global-cache test with a deterministic pure-map test. Reasons: one authority for each policy, honest UI, fail-closed channel revocation, and stable tests.

TDD evidence: missing clear_all_extensions first failed to compile; the dialog test first exposed the forbidden persistent button; the SDK contract assertion first failed on optional effect. All became green after the corresponding minimal corrections. One incorrect Cargo filter executed zero tests and was explicitly rejected as evidence; the exact listed test was then run 1/1. An interrupted validation launch was also not counted.

Fresh final verification by the controller: cargo test --manifest-path src-tauri/Cargo.toml => 4622 library passed, 10 ignored, 0 failed, plus integration target passed; npm test => Vitest 2834/2834, build scripts 56/56, CI scripts 16/16, CEF 45 passed and 1 platform skip, E2E runner 70/70, extension Host 57/57, reasoning fixtures green; strict Clippy with -D warnings green; cargo fmt --check green; npm run lint green; npx tsc --noEmit green; git diff --check green; no changed production file exceeds 230 lines; graphify update . completed at 41744 nodes, 87089 edges, 2382 communities.

Known deliberate boundaries for following lots: fingerprint-driven grant revocation is P0.4; P1.1 will narrow the current global runtime-unavailable revocation to the failed Host identity. The macOS __eh_frame linker warning is pre-existing and non-failing. No Office feature behavior, MCP, external application, marketplace, or 10 percent discovery policy was changed.
