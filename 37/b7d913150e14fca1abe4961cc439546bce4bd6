JALON 3 - PASSE 3 DE REVIEW, REPRISE ET STOCKAGE

PERIMETRE

- Corrige le temporaire de recu de bundle orphelin, le faux test de convergence, le rouge Clippy Windows et les quatre importants restants de la review.

- Aucune preuve native, CI, fusion ou refonte hors de cette liste.

CORRECTIONS DE REPRISE

- Une autorite temporary_recovery traite, un document durable par passage, les temporaires bornes du recu de bundle, du marqueur de migration et du journal.

- Un temporaire de recu de bundle regulier est retire avant adoption seulement si le recu final est absent ou lisible; symlink, dossier, surdimensionnement et recu final invalide restent fermes.

- La matrice de coupures exige RecoveryOutcome::Ready, un layout terminal Ready et la presence attendue du bundle actif; un etat bloque stable ne peut plus passer pour une convergence.

- Inventaire write_new_atomic relu: journal, marqueur, recu processus et recu bundle ont une reprise explicite; VERSION.tmp reste confine dans un staging non autoritaire supprime comme un ensemble apres interruption.

CORRECTIONS UNIX ET DIAGNOSTIC

- DirectoryStream possede DIR* par RAII et le ferme sur succes comme sur chaque sortie d erreur.

- readdir distingue fin de flux et errno, et les huit appels libc conservent operation et code OS.

- Le compteur de preuve est local au fil de test pour ne pas devenir instable en execution parallele.

- Adoption, recu de bundle, archives et inspections cites par la review utilisent storage_error sans perdre kind, operation ou code OS.

CONFINEMENT DES MODELES

- Le fallback starts_with/strip_prefix est supprime sur Unix et Windows.

- Un chemin absent conserve une ancre d identite native et une suite bornee de composants valides; les relations sont decidees depuis cette ancre, jamais depuis un prefixe de chemin complet.

- Windows compare les composants absents avec CompareStringOrdinal et ses regles natives sans casse.

- L auto-review a detecte et corrige avant commit le cas Models/models sous Windows.

TDD OBSERVE

- Rouge initial: AdoptLegacyActive Write before=false obtenait Err(OllamaStorageUnavailable) lors de la reprise, preuve du temporaire de recu orphelin.

- Rouge local pendant l auto-review: import de type manquant apres le passage des comparaisons en Result, corrige avant validation.

- La compilation croisee Windows depuis macOS s arrete dans libz-sys/zstd-sys faute de SDK C Windows, avant Beaver; le runner Windows reste l autorite native.

PREUVES VERTES OBSERVEES SUR MACOS

- Filtre officiel services::ollama_manager: 307 passes, 0 echec, 1 generateur volontairement ignore, 308 filtres verifies.

- recovery_tests: 25 passes; durable_fs_tests: 26 passes; spawn_profile_tests: 21 passes.

- cargo clippy --all-targets -- -D warnings: vert.

- cargo fmt --all -- --check: vert; git diff --check: vert.

- Production manager <= 229 lignes.

- graphify update .: vert, 34452 noeuds, 70574 aretes, 2041 communautes.

LIMITES ET SUITE

- La correction Windows CompareStringOrdinal sera compilee et executee par le runner Windows.

- Les preuves natives v1.1.2 sur macOS, Linux et Windows, interruption/reprise et fermeture coordonnee restent volontairement apres code, CI et review verts.

- Pousser cette tete et attendre un verdict CI complet avant toute autre correction.

CI COMPLETE OBSERVEE APRES PUSH

- PR #19, tete 37b7d913150e14fca1abe4961cc439546bce4bd6.

- Run principal 31944539149: Backend Rust, Linux WebView E2E, Windows CEF, Windows tests, macOS CEF, Frontend et Desktop E2E tous verts.

- Windows CEF natif a termine vert en 29m42s; macOS CEF natif vert en 15m38s.

- Analyses actions, JavaScript/TypeScript, Rust, CodeQL, GitGuardian, RustSec et npm vertes.

- Aucun job rouge, annule ou relance; verdict attendu jusqu a completion avant cette note.
