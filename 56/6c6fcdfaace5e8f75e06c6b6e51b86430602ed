Task 9 fix round 2; base c367dfd3f1bd2a284162be3437992e2df35f786c. Scope limited to the two Important findings in task-9-review.md; no Task 10+, Task 14, global suite, CI, push, or merge.

TDD reds observed before production: the Windows source-contract test failed because remove_tree_verified still contained remove_tree(root.path()); the real recovery table first failed convergence at CommitFreshInstallFromActive before=true with active Absent, update_staging Present, backup Present. Both failures were corrected and rerun.

Windows verified deletion now delegates remove_tree_verified to durable_fs_windows_verified. It holds the canonical root handle and native identity, opens the volume and children by FileId with OpenFileById, revalidates volume/file identity and directory/reparse attributes for every entry, recursively deletes through SetFileInformationByHandle, rejects reparse points and malformed entries, and bounds depth to 64, entries to 8192, enumeration buffer to 64 KiB, and names to 16K UTF-16 units. UNC/non-drive volume resolution fails closed. No shell and no remove_dir_all path fallback in the verified method.

The Windows contract has an always-built static test and a cfg(windows) native nested-tree test; native Windows execution was not available on macOS. Cross-target cargo check was attempted: GNU failed in cef-dll-sys with Unsupported target triplet: x86_64-pc-windows-gnu; MSVC failed before crate checking because the macOS host lacks Windows SDK headers (stdlib.h/windows.h/string.h) while compiling aws-lc-sys/zstd-sys. These are recorded limitations, not green Windows evidence.

Real recovery cutpoint table wraps PlatformOllamaDurableFs and public recover on real temporary layouts. It covers RemoveUncommittedInstallStaging, CommitFreshInstallFromActive, CommitFreshInstallFromUpdate, RestoreLegacyBackup, RestoreLegacyBackupFromModernBackup, target validation, rollback cleanup-pending persistence, both failed transitions, completed journal removal, and legacy adoption; before/after rename, sync, remove, write, and publish boundaries are injected where each decision uses them. After every failure the test clears the fault and requires two consecutive equal layout snapshots, plus explicit cutpoint event coverage.

Verification after correction: services::ollama_manager::recovery_tests 14 passed/0 failed; services::ollama_manager::durable_fs_tests 19 passed/0 failed; cargo check passed; cargo clippy --all-targets -- -D warnings passed; cargo fmt --all -- --check passed; git diff --check passed; graphify update . passed with the known 73 zero-node warnings. Production files touched by this fix: durable_fs_windows.rs 218, durable_fs_windows_verified.rs 53, entries.rs 180, handles.rs 130, path_identity_windows_handles.rs 45; all below 230. Commit 566c6fcdfaace5e8f75e06c6b6e51b86430602ed.

Additional targeted filter evidence after commit: services::ollama_manager::cleanup_tests ran 4 passed/0 failed; services::ollama_manager::rollback_tests ran 5 passed/0 failed. The full round-2 report and progress ledger record these results alongside recovery 14/14 and durable_fs 19/19.
